{
  "name": "wayfare-monorepo",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm -r dev:build && pnpm --parallel -r dev:watch",
    "check": "biome check",
    "fix": "biome check --write"
  },
  "packageManager": "pnpm@9.1.1",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@webgpu/types": "^0.1.52"
  }
}
